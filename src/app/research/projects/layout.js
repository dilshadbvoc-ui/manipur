import { createMetadata } from '@/lib/metadata';
export const metadata = createMetadata({ title: 'Research Projects', description: 'Ongoing and completed research projects at Manipur International University. Explore funded research and innovation initiatives.', path: '/research/projects' });
export default function Layout({ children }) { return children; }
