import { createMetadata } from '@/lib/metadata';
export const metadata = createMetadata({ title: 'Research Publications', description: 'Academic publications and research papers by MIU faculty and students. Explore scholarly contributions from Manipur International University.', path: '/research/publications' });
export default function Layout({ children }) { return children; }
