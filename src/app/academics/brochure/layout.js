import { createMetadata } from '@/lib/metadata';
export const metadata = createMetadata({ title: 'Brochure Download', description: 'Download MIU brochures for all schools and programs. Get detailed information about courses, fees and admissions at Manipur International University.', path: '/academics/brochure' });
export default function Layout({ children }) { return children; }
