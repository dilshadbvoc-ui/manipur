import { createMetadata } from '@/lib/metadata';
export const metadata = createMetadata({ title: 'MIUNEST Student Portal', description: "MIUNEST - Manipur International University's integrated student portal for academic records, fee payments, exam schedules and campus services.", path: '/miunest' });
export default function Layout({ children }) { return children; }
