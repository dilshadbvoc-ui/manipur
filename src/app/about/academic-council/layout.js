import { createMetadata } from '@/lib/metadata';
export const metadata = createMetadata({ title: 'Academic Council', description: 'IInd Academic Council of Manipur International University. 60 members including faculty, government nominees, industry experts and student representatives.', path: '/about/academic-council' });
export default function Layout({ children }) { return children; }
