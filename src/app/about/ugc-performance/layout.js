import { createMetadata } from '@/lib/metadata';
export const metadata = createMetadata({ title: 'UGC Performa', description: 'UGC Performa and mandatory disclosures of Manipur International University as per University Grants Commission guidelines.', path: '/about/ugc-performance' });
export default function Layout({ children }) { return children; }
